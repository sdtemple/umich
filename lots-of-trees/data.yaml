folder: 'data'
yaml: 'data.yaml'

files:
  experiments: 'train.tsv'

# number of markers in genotype matrix
num_markers: '2'

# old parameters
trim_trees: '2' # don't analyze these trees at end of chromosomes
num_trees: '5' # only analyze these first trees processed serially
